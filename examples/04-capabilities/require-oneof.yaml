apiVersion: orionmesh.dev/v1
kind: Service
metadata: { name: doc-viewer }
spec:
  runtime: { kind: docker, image: doc-viewer:latest }
  requires:
    search:
      dataset: amiga_schematics
      format: [pdf, png]             # OneOf — JSON array
